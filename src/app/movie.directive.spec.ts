import { MovieDirective } from './movie.directive';

describe('MovieDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieDirective();
    expect(directive).toBeTruthy();
  });
});
